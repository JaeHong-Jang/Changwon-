# 연구 계획서 — 하네스 구조

> **프로젝트**: 창원시 폭우 침수·하수 역류 우선 대응지역 분석
> **기간**: 2026.08.18 ~ 09.30 (6주)
> **팀**: 2~3명
> **작성일**: 2026.08.18 (딥리서치 3건 기반) · **개정**: 2026.08.18 AI 요소 재배치(분석 도구화)

> **포지셔닝 원칙 (2026.08.18 결정)**: 이 대회는 **데이터 분석 공모전**이다(제출물=분석보고서, 심사=창의성·정책 실현성·데이터 분석 적정성). AI는 **분석 안에서 필요한 자리에만** 쓴다 — ① 민원 텍스트 LLM 구조화(Layer 2 입력), ② XGBoost/SHAP 역류 예측. 알림 Agent는 활용방안 챕터의 **출력부 예시 1개(≤1p)**, 시민 챗봇은 **삭제**. 보고서 비중 = 분석 75 / 활용·Agent 15 / 개요·출처 10.

---

## 0. 하네스 구조란

이 계획은 "할 일 목록"이 아니라 **증거 게이트로 연결된 스토리 체인**입니다.

```
Story N  ──[증거 제출]──▶  checkpoint  ──▶  Story N+1 활성화
              │
              └─ 증거 없으면 완료 불가 (goals.py가 거부)
```

| 원칙 | 구현 |
|------|------|
| 각 스토리 = 목표 + 입력 + 산출물 + **검증 기준** | 이 문서 §4 |
| 다음 스토리는 이전 스토리 checkpoint 후에만 시작 | `goals.py next` |
| 완료 주장에는 반드시 증거(파일 경로·수치·로그) | `goals.py checkpoint --evidence` |
| 결정은 기록으로 남김 | `docs/decisions/NNN-*.md` |
| 분기점(Decision Gate)은 증거 기반으로 Plan A/B 선택 | G006 |
| 최종 스토리는 검증 명령 없이 완료 불가 | `--verify-cmd` 게이트 |
| 상태는 파일로 영속화 → 세션·사람이 바뀌어도 이어감 | `.fablize/goals.json` + git |

**운영 명령**
```bash
# 저장소 루트에서
python3 /mnt/c/Users/User/.claude/plugins/fablize/scripts/goals.py status      # 현재 상태
python3 /mnt/c/Users/User/.claude/plugins/fablize/scripts/goals.py next        # 다음 스토리 활성화 + 핸드오프 출력
python3 /mnt/c/Users/User/.claude/plugins/fablize/scripts/goals.py checkpoint --id G00N --status complete --evidence "..."
```
> Windows 팀원: `python C:\Users\User\.claude\plugins\fablize\scripts\goals.py ...` (경로만 다름)

---

## 1. 딥리서치 결론 → 계획에 반영된 것

계획을 세우기 전에 3건의 리서치를 돌렸고, 그 결과가 아래 설계를 바꿨습니다.

### 1-1. 데이터 실현 가능성 검증 (가장 중요)

| 데이터 | 검증 결과 | 계획 반영 |
|--------|----------|----------|
| 하수관로 설치현황 (환경공단) | ❌ 처리시설×행정구역 **집계** — 관로별 좌표·연도 없음 | Layer 2는 정보공개청구 회신에 의존 → **G006 Decision Gate**에서 Plan A/B 분기 |
| 창원시 건축물대장 | ❌ 주차대수 데이터 — 사용승인일·지하층수 없음 | **건축HUB API**(국토부)로 경로 변경 |
| 시간별 강수량 (창원시) | ✅ **시 자체 관측망 ~50지점, 10년치 191,340행** | Layer 1의 뼈대. 관측지점 좌표 확보가 선결(G003) |
| 하천수위 통계 (창원시, 신규 발견) | ✅ 50개 관측소 시간대별 | Layer 1 검증 변수 |
| 배수펌프장 (창원시) | ✅ 9건, 주소·설치연도 | 지오코딩 → "펌프장 서비스 거리" 변수 |
| SGIS 격자통계 (통계청) | ✅ 100m/500m 격자 인구 존재 | Layer 3 직접 결합 |
| DEM (국토지리정보원) | ✅ 5m, 회원가입 후 다운로드 | 경사·상대고도 |
| 토지피복도 (환경부 EGIS) | ✅ SHP, **승인 1~3일** | 불투수면. 첫날 신청(G001) |
| 침수흔적도 | ⚠️ 공개본은 WMS 열람만 | 정보공개청구에 SHP 추가 요청 |

### 1-2. 방법론

| 발견 | 계획 반영 |
|------|----------|
| **국토부 「도시 기후변화 재해취약성분석 지침」**: 취약성 = 기후노출 × 도시민감도, **100m 격자**(2024 개정), Jenks I~IV | Layer 1·3을 이 구조로 → "창원시 실무와 동일 표준" = 정책 실현성 근거 |
| **IPCC AR5**: Risk = Hazard × Exposure × Vulnerability | 통합 지수 수식 |
| **창원시 선행연구 2편** (최유라·한우석 2024 국토연구; 박기용 외 2017) — 검증 사례지: 마산회원구 양덕동·봉암동, 의창구 팔용·명서·사화동, 동읍 덕산리 | Layer 1 검증용 ground truth + "선행연구는 도시계획 관점, 격자 예측·AI 결합은 공백" 차별화 |
| 가중치: 실무 표준은 표준화 후 합산(동일가중), 학술 차별화는 엔트로피 (이선미 외 2020) | 기본 동일가중 + 엔트로피 민감도 분석 → "가중치에 강건" 주장 |
| 등급: Balica(2013) 5등급 (음정인·김형규 2024) | 최종 등급 체계 |
| 관로노후×강우→민원 ML은 **학술 공백** | Layer 2 Plan A의 독창성 근거 |

### 1-3. 수상작 패턴

| 발견 | 계획 반영 |
|------|----------|
| ⚠️ **2022 창원 우수상 = "침수예측모델(LGBM)"** 이미 존재 | 보고서 서두에 **차별화 비교표** 필수 (1차 "중복 여부" 심사 대응) |
| 2025 창원 1위 후보 = sLLM 민원응대(민원 데이터를 LLM으로 다룸, 인건비 1,200만원 절감 정량화); 2026 경남 "AI 에이전트" 대상은 **AI 창업 부문**(분석 부문 대상은 순수 분석) | LLM은 **분석 도구**(민원 텍스트 구조화)로만 사용, Agent는 출력부 예시 1개 + 정량 효과 1줄. "분석 옆에 챗봇"으로 수상한 사례 없음 |
| 최우수작 공통: 창원시 데이터포털 데이터 + "TOP N / 최적 입지" + 자체 지수 정의 + 정책 연계 | "창원 배수인프라 위험지수(CDRI)" 명명 + TOP 20 격자 + 담당부서·예산 명시 |
| 2차 심사에 **관련 부서(하수도사업소·재난안전과)** 직접 참여 | 78억 하수도 정비 예산, 3개 하수센터, 재난안전대책본부 TF에 "끼워 넣는" 시범안 |
| 2026 공식 서식 목차: 개요 → 데이터·툴·알고리즘·**스토리보드** → 해석 → 활용·기대효과 → 출처 | 보고서 장 제목을 그대로 따름. 스토리보드 = 분석 파이프라인 흐름도(+알림 예시 화면 1장) |
| 권장 배분 16~19매, 지도 6~8장, 표 4~6개 | G013 체크리스트 |

---

## 2. 연구 설계 (확정)

### 2-1. 주제 확정문 · 연구 질문

**주제명**: 창원시 폭우 침수·하수 역류 우선 대응지역 분석 — 100m 격자 통합위험지수(CDRI) 기반 TOP 20 선정
**한 문장**: 강수·하천수위·배수펌프장·하수관로·민원·인구·건물 데이터를 100m 격자로 결합해 침수×역류×취약계층 통합 위험지수를 산출하고, 폭우 시 먼저 점검·대응할 지역 20곳과 그 이유(기여도)를 제시한다.

> 폭우 시 창원시에서 **침수·하수 역류 위험이 높고 취약계층이 밀집한 격자**는 어디이며(TOP 20), 그 우선순위를 하수도사업소·재난안전 부서가 **점검·대응 순서**로 어떻게 쓸 수 있는가?

### 2-2. 분석 프레임 — 국토부 지침 + IPCC AR5

```
공간 단위: 100m 격자 (국토부 2024 개정 지침 준수, SGIS 격자인구와 직접 결합)

Layer 1  침수 취약성  = 기후노출(강우 IDW 보간) × 민감도(저지대·경사·불투수면·하천버퍼·펌프장거리)
Layer 2  역류 위험    = [전처리] 민원 텍스트 수천 건 → LLM 구조화(위치·유형·긴급도) → 격자 라벨  ← AI가 '분석 도구'로 쓰이는 자리
                       Plan A: 관로 경과연수·관경·재질·밀도 + 강우 → XGBoost(타깃=민원) + SHAP
                       Plan B: 처리구역 합류식비율 × 관로밀도 × 동별 민원건수 → 노후도 점수 (rule-based)
Layer 3  취약계층     = 65세↑·1인가구(SGIS) + 노후건물·지하층(건축HUB) − 적응역량(대피소·펌프장 접근성)

통합     CDRI = Hazard(L1+L2 정규화) × Exposure(인구밀도·건물수) × Vulnerability(L3)
         → 0~1 정규화 → Balica 5등급 → TOP 20 격자 + 기여도 분해(침수/역류/취약 %)
가중치   기본 동일가중(지침) + 엔트로피·AHP 민감도 → 순위 상관(Spearman) 보고
검증     Layer 1: 선행연구 사례지 5곳 + 시민의소리 침수 민원 위치 적중률
         Layer 2: (A) 홀드아웃 F1/AUC + SHAP  (B) 민원 상위 동과의 순위 상관
```

### 2-3. AI 요소의 자리 (분석 도구 우선, 장식 금지)

| 요소 | 성격 | 보고서 위치 | 검증/측정 |
|------|------|------------|-----------|
| **민원 텍스트 LLM 구조화** (`classify_complaint`) | 정보공개청구로 받는 민원 원문(수천 건)을 사람이 못 읽음 → LLM으로 위치·유형(악취/역류/침수)·긴급도 추출 → Layer 2 라벨·검증 세트 | **3장 방법론** (데이터 전처리) | 수작업 라벨 100건 대비 정확도(%) 표 |
| **XGBoost + SHAP 역류 예측** (Plan A) | Layer 2 모델 자체 | **3장 방법론·결과** | 홀드아웃 F1/AUC, SHAP 상위 변수 |
| 호우특보 → 담당자 알림 초안 (`draft_alert`) | 분석 결과(TOP 20)의 **출력부** 예시. 특보 시 상위 격자·펌프장·하수센터를 묶어 알림 문안 생성 | **6장 활용방안** ≤1p (화면 1장 + "특보→알림 N초") | 실행 로그 1건 |
| ~~시민 안내 챗봇~~ | 장식 → **삭제** | — | — |

구현 최소치: `src/agent/classify_complaint.py`(LLM 배치 분류, 캐싱) + `src/agent/draft_alert.py`(템플릿+LLM). Streamlit 데모는 **옵션**(G011, 시간 남을 때만).

### 2-4. 차별화 (2022 우수상 대비) — 보고서 서두 표

| | 2022 우수상 침수예측모델 | 본 연구 |
|--|------------------------|--------|
| 대상 | 침수 여부(0/1) 예측 | 침수 + 하수 역류 + 취약계층 **통합 위험지수** |
| 결과물 | 예측 성능 | **우선 대응 TOP 20 격자** + 기여도 분해 |
| 민원 데이터 | 미사용 | 정보공개청구 민원 원문 → **LLM 구조화** → 검증·라벨 |
| 실행 | — | 부서별 점검 우선순위 + 특보 연동 알림 예시 |
| 표준 | 자체 | 국토부 지침 + IPCC AR5 프레임 |
| 정책 | — | 하수도사업소 78억 정비·재난안전 TF 연계 3개월 시범안 |

---

## 3. 역할 분담 (2~3명)

| 역할 | 담당 스토리 | 핵심 스킬 |
|------|-----------|----------|
| **R1 데이터·공간분석** | G002, G003, G004, G007, G009, G010 | geopandas, rasterio, 격자·보간 |
| **R2 모델·LLM** | G008, G011(옵션), G012 | XGBoost/SHAP, Claude API(민원 분류·알림 초안), Folium |
| **R3 문서·검증** (2명이면 R1+R2가 분담) | G001, G005, G006, G013, G014 | 신청·문의, 결정기록, 보고서, 검증 |

주간 리듬: **매주 월요일** `goals.py status` → 리스크 점검 → 다음 스토리 활성화. 스토리 완료 시 즉시 checkpoint + git commit.

---

## 4. 스토리 체인 (14 스토리 · 7 페이즈)

> 각 스토리의 **검증 기준**이 곧 checkpoint `--evidence`에 들어갈 내용입니다.

### Phase 0 · 착수 (8/18~8/20)

**G001 데이터 접근 일괄 신청** — 담당 R3 · 기한 8/18
- 입력: `docs/references/정보공개청구_문안.md`
- 할 일: ① 정보공개청구(민원 3년 + 관로 연도·좌표 + 침수흔적도 SHP) 정부24 접수 ② EGIS 토지피복도 자료신청 ③ 국토지리정보원 회원가입·DEM 다운로드 신청 ④ SGIS OpenAPI 인증키 ⑤ 건축HUB API 활용신청 ⑥ 기상청 API 활용신청 → `.env`
- 산출물: `docs/data_access_log.md` (신청일·접수번호·예상 회신일)
- **검증**: 6건 접수번호 또는 스크린샷 경로 기재
- 리스크: EGIS 승인 1~3일 → 첫날 신청 필수

### Phase 1 · 데이터 확보·검증 (8/18~8/31)

**G002 즉시 확보 데이터 다운로드 + 스키마 검증** — R1 · 8/22
- 입력: 강수량(15150315), 하천수위(창원포털 seq=772), 배수펌프장, 하수관로 집계(15118453), SGIS 격자통계·경계(15141768), 전국하천표준(15139206), 시민의소리 침수·하수 민원 스크래핑
- 산출물: `data/raw/*` + `data/raw/README.md` 표 완성 + `src/data/validate_raw.py`
- **검증**: 검증 스크립트 출력(파일별 shape·컬럼·결측률) 로그 첨부; 강수량 191,340행 ±, 지역명 유니크 수 확인

**G003 관측지점 좌표 확보** — R1(+R2 지오코딩) · 8/25
- 입력: 강수량·하천수위 지역명/코드
- 할 일: 코드북 탐색 → 없으면 지명 지오코딩(카카오/VWorld) → 미해결분 재난안전대책본부 문의
- 산출물: `data/processed/stations.csv` (지점명, lat, lon, 출처)
- **검증**: 50지점 중 좌표 확보율 ≥ 90% + 지도 1장 (`reports/figures/stations_map.png`)
- 리스크: 미확보 시 기상청 ASOS 창원(155)+AWS로 대체 (해상도 저하 기록)

**G004 분석 격자 구축** — R1 · 8/31
- 입력: DEM, 토지피복도, 하천, 펌프장(지오코딩), SGIS 격자인구, 행정경계
- 할 일: 100m 격자 생성 → 격자별 평균고도·상대고도·경사·불투수면비율·하천100m버퍼·최근접펌프장거리·인구 결합
- 산출물: `data/processed/grid_100m.gpkg` + `docs/data_dictionary.md`
- **검증**: 격자 수, 변수별 결측률 <5%, 요약통계표; 03 노트북 Restart&Run All 통과
- 리스크: EGIS 지연 → SGIS 격자 건물면적 비율로 불투수 근사(대체 기록)

### Phase 2 · EDA (8/25~9/5)

**G005 탐색적 분석** — R3(+R1) · 9/5
- 할 일: 강수 극값(10년 시간최대, 80mm↑ 일수, 2024.9 500mm 이벤트 프로파일), 하천수위-강수 반응 지연, 격자 변수 분포·상관, 침수 민원 위치 vs 저지대
- 산출물: `notebooks/02_eda.ipynb`, `reports/figures/eda_*.png` (≥6장)
- **검증**: 노트북 Restart&Run All 통과 + figure 6장 경로 + "핵심 발견 5줄" (`docs/eda_findings.md`)

### Phase 3 · Decision Gate (9/1~9/3)

**G006 Layer 2 설계 확정** — R3+R2 · 9/3
- 입력: 정보공개청구 회신 (또는 미회신)
- 분기 규칙:
  - 관로별(좌표+연도) 회신 → **Plan A** XGBoost
  - 동 단위 집계 or 민원만 회신 → **Plan B** 근사 노후도 점수 + 민원 핫스팟
  - 미회신 → Plan B + "회신 시 Plan A 확장" 명시, 재청구
- 산출물: `docs/decisions/001-layer2-design.md` (근거·선택·영향)
- **검증**: 결정 문서 + 회신 파일 경로(또는 미회신 근거)

### Phase 4 · 모델링 (9/1~9/15)

**G007 Layer 1 침수 취약성** — R1 · 9/8
- 할 일: 기후노출(강우 IDW → 격자) × 민감도(지형·불투수·하천·펌프장) 표준화 합산 → Jenks I~IV
- 검증 세트: 선행연구 사례지 5곳(양덕·봉암·팔용·명서·사화) + 시민의소리 침수 민원 위치
- 산출물: `data/processed/layer1_flood.gpkg`, `reports/figures/layer1_map.png`
- **검증**: 사례지 5곳 중 I·II등급 적중 수, 민원 위치 상위 20% 격자 포함률 (AUC 또는 적중률 수치)

**G008 Layer 2 역류 위험** — R2 · 9/12
- 공통(민원 원문 수령 시): `classify_complaint` LLM 배치 → 위치 지오코딩·유형·긴급도 → 격자 라벨 (`data/processed/complaints_labeled.csv`), 수작업 100건 대비 정확도 표
- Plan A: 격자 피처(경과연수·관경·재질·밀도·펌프장거리·시간최대강우·저지대) → XGBoost 이진분류 → SHAP
- Plan B: 처리구역 합류식비율 × 관로밀도 × 동별 민원건수 → 0~1 점수
- 산출물: `data/processed/layer2_sewer.gpkg`, `reports/figures/layer2_map.png` (+ A: SHAP summary)
- **검증**: LLM 분류 정확도(%) + A → 홀드아웃 F1·AUC 표 / B → 민원 상위 10개 동과 점수 상위 동 Spearman ρ

**G009 Layer 3 취약계층** — R1 · 9/10
- 할 일: SGIS 65세↑·1인가구 밀도, 건축HUB 사용승인 30년↑·지하층 건물 수(창원 5개 구 법정동 순차 호출·캐싱), 대피소·펌프장 접근성(적응역량, 음의 부호)
- 산출물: `data/processed/layer3_vuln.gpkg`, `reports/figures/layer3_map.png`
- **검증**: 건축HUB 수집 건수·커버리지, 변수 요약통계, 지도

**G010 통합 위험지수 CDRI + TOP 20** — R1+R2 · 9/15
- 할 일: R = H(L1,L2) × E × V → 0~1 → Balica 5등급; 가중치 3방식(동일·엔트로피·AHP) 민감도; TOP 20 격자 + 기여도 분해 + 행정동·도로명 라벨
- 산출물: `data/processed/cdri.gpkg`, `reports/tables/top20.csv`, `reports/tables/sensitivity.csv`, `reports/figures/cdri_map.png`
- **검증**: TOP 20 표(격자ID·동·CDRI·등급·기여도%), 가중치 방식 간 TOP 20 Spearman ρ, 04 노트북 통과

### Phase 5 · 알림 출력부 (9/15~9/20) — **옵션 스토리**

**G011 특보 연동 알림 초안 (옵션)** — R2 · 9/20
- 조건: G010 완료 후 여유 있을 때만. 분석 일정이 밀리면 **건너뛰고 checkpoint에 "skipped: 일정" 기록** → 보고서 6장은 텍스트 예시로 대체.
- 할 일: `draft_alert(top_grids, forecast)` → 기상청 특보 입력 → TOP 5 격자·담당 하수센터·펌프장 포함 알림 문안 생성. 화면 1장 + 실행 로그.
- 산출물: `src/agent/draft_alert.py`, `reports/figures/alert_example.png`, 소요시간(초)
- **검증**: 실행 로그 1건 + 화면 1장 (Streamlit·챗봇 없음)

### Phase 6 · 시각화·보고서 (9/15~9/28)

**G012 보고서용 시각화 확정** — R2 · 9/22
- 할 일: Folium 통합 지도(레이어 토글), 레이어별 정적 지도, TOP 20 라벨 지도, 민감도 차트, 강수 이벤트 차트, 분석 파이프라인 흐름도(스토리보드용)
- 산출물: `reports/figures/final_*.png` (8장), 05 노트북
- **검증**: 8장 파일 목록 + 05 노트북 Restart&Run All 통과

**G013 보고서 v1 → 리뷰 → v2** — R3(+전원) · 9/28
- 목차(2026 공식 서식): 1 분석 개요(2p, 차별화 표 포함) → 2 데이터·전처리(2~3p, 출처 URL 표) → 3 방법론·결과(7~8p, LLM 민원 구조화·Layer 1~3·CDRI·민감도·검증) → 4 해석·시사점(2p, TOP 20 표+원인) → 5 활용·기대효과(2~3p, 담당부서·3개월 시범·알림 예시 ≤1p·정량효과) → 6 출처(1p). **분석 75 / 활용 15 / 개요·출처 10**
- 산출물: `reports/final/보고서_v1.pdf` → 팀 리뷰(다른 사람이) → `보고서_v2.pdf`
- **검증**: 페이지 수 16~19, 체크리스트(`docs/report_checklist.md`) 전 항목 ✓, 리뷰어 코멘트 반영 기록

### Phase 7 · 최종 검증·제출 (9/28~9/30) ★ 검증 게이트

**G014 최종 검증 + 제출** — 전원 · 9/30 18:00
- verify-cmd: `jupyter nbconvert --execute notebooks/0{1..5}_*.ipynb` 전체 통과 + `pdfinfo 보고서_final.pdf` 페이지 수 + 출처 URL 전수 접속 확인 스크립트
- 할 일: 참가신청서 작성, 창원시 홈페이지 온라인 접수, 접수 확인
- **검증**: `--verify-cmd` 실행 결과 + 접수 확인 스크린샷 (게이트 없이는 완료 불가)

---

## 5. 리스크 레지스터

| # | 리스크 | 확률 | 영향 | 대응 | 감지 시점 |
|---|--------|------|------|------|----------|
| R1 | 정보공개청구 미회신/집계만 | 중 | 상 | Plan B (G006), 재청구, "회신 시 확장" 명시 | 9/1 |
| R2 | 관측지점 좌표 미확보 | 중 | 상 | 지오코딩 → 문의 → ASOS/AWS 대체 | G003 |
| R3 | EGIS 토지피복 승인 지연 | 중 | 중 | SGIS 건물면적 비율로 불투수 근사 | G004 |
| R4 | 건축HUB 일 10,000건 제한 | 높 | 중 | 법정동 순차·캐싱, 5일 분산 | G009 |
| R5 | 1차 심사 "2022 침수모델과 중복" | 중 | 상 | 서두 차별화 표 + 통합지수·취약계층·민원 LLM 구조화 강조 | G013 |
| R6 | 20매 초과 | 중 | 상 | 코드·부록은 GitHub 링크, 본문 압축 | G013 |
| R7 | AI가 "장식"으로 읽힘 | 중 | 중 | LLM은 방법론 안(민원 구조화)에서만, 챗봇 없음, Agent ≤1p·옵션 | G013 |
| R8 | 팀원 이탈/일정 지연 | 낮 | 상 | 스토리 단위 인수인계(`.fablize/` + decisions) | 매주 월 |

---

## 6. 산출물 맵

```
docs/
├── RESEARCH_PLAN.md          ← 이 문서 (계획의 원본)
├── data_access_log.md        ← G001
├── data_dictionary.md        ← G004
├── eda_findings.md           ← G005
├── decisions/
│   ├── README.md
│   └── 001-layer2-design.md  ← G006
└── report_checklist.md       ← G013
data/processed/
├── stations.csv              ← G003
├── grid_100m.gpkg            ← G004
├── layer1_flood.gpkg         ← G007
├── complaints_labeled.csv    ← G008 (LLM 구조화)
├── layer2_sewer.gpkg         ← G008
├── layer3_vuln.gpkg          ← G009
└── cdri.gpkg                 ← G010
reports/
├── figures/                  ← G005, G007~G012
├── tables/top20.csv, sensitivity.csv  ← G010
└── final/보고서_v1.pdf, v2.pdf, final.pdf  ← G013, G014
src/agent/classify_complaint.py        ← G008 (LLM 민원 구조화)
src/agent/draft_alert.py               ← G011 (옵션)
.fablize/goals.json, ledger.jsonl      ← 하네스 상태 (git 추적)
```

---

## 7. 필수 인용 문헌

1. 국토교통부, 「도시 기후변화 재해취약성분석 및 활용에 관한 지침」 (2024 개정) — https://law.go.kr/LSW/admRulInfoP.do?admRulSeq=2100000107729
2. 최유라·한우석 (2024). 용도지역별 폭우재해 취약지역의 재해예방형 도시계획 수립방안: 창원시를 중심으로. 국토연구 120, 77-95.
3. 박기용·정진호·전원식 (2017). 창원시 용도지역별 침수 피해에 따른 위험등급화 분석. 한국산학기술학회논문지 18(4), 685-693.
4. 이선미·최영제·이재응 (2020). 엔트로피 가중치 산정방법을 활용한 도시지역 홍수취약성 평가. 한국방재학회논문집 20(6), 389-397.
5. 음정인·김형규 (2024). 서울특별시 반지하 가구를 고려한 홍수 취약성 평가. 기후변화연구 15(1), 35-46.
6. IPCC (2014). AR5 WGII — Risk = f(Hazard, Exposure, Vulnerability).
7. 환경부 (2025). 하수관로·맨홀 조사 및 상태등급 판단기준 표준매뉴얼.
8. 경남일보 (2026). 창원시, 올해 하수도 민원 7900건 접수 처리. https://www.gnnews.co.kr/news/articleView.html?idxno=626103
