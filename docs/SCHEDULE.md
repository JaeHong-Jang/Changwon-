# 남은 일 — 2026-09-08 ~ 09-30 (마감 9/30 18:00)

> 앞으로 할 일만 적는다. 지금까지의 경과는 `PROJECT_STATUS.md`, 방법론은 `METHODOLOGY.md`,
> 팀원 지시서는 `TEAM_TASKS.md`.
>
> **한 줄 요약: 분석 본선은 끝났다. 남은 건 ① 승인 ② 9/14 검증 ③ 보고서다.**

---

## 지금 당장 (오늘, 10분) — 본인

승인 3건. 이게 없으면 아래 모든 하류가 닫혀 있다.

```bash
python -m src.pipeline approve h03_pump_stations --by 안도경 --note "공식 15 + 지오코딩 2 지도 확인"
python -m src.pipeline approve h07_cdri          --by 안도경 --note "산식·가중치 확정, tier 모드 수용"
python -m src.pipeline approve h08_top20_policy  --by 안도경 --note "우선대응 20곳·정책카드 승인"
```

**승인 전에 볼 것** — `reports/tables/sensitivity.csv` 와 `reports/tables/top20.csv`.
h07·h08 승인은 "이후 재조정 금지"라는 약속이다. 표를 보고 납득한 뒤에 한다.

---

## 발견된 문제 — 결정 003 과 구현이 어긋난다 (본인, 9/9)

브랜치를 합치면서 `dev` 에만 있던 **결정 003(CDRI 5등급 체계 R1~R5)** 을 발견했다.
2026-08-19 에 채택된 결정인데 현재 `src/stages/h07_cdri.py` 가 이를 따르지 않는다.

| 결정 003 이 정한 것 | 현재 구현 |
|---|---|
| 최종 등급 **R1~R5 오름차순** (5 = 최위험), 명칭은 행동 기준 (관찰/일반관리/점검강화/우선대응/최우선대응) | Balica 라벨 (매우낮음~매우높음) |
| 침수흔적 양성 격자 <100 이면 **Jenks 5등급이 본안**, Balica 는 국제 비교용 **병기** | Balica 가 본안, Jenks 는 비교 |
| `grade_raw` / `grade_final` 분리 (검증은 raw, 대응표는 final) | 없음 |
| 규칙 A (L1 백분위 ≥99 → 한 단계 상향, R3 미만이면 R3까지) | 없음 |
| 규칙 B (V 상위 10% **그리고** H 상위 30% → 한 단계 상향) | 없음 |
| 규칙 C (기지정 지역 200m 내 → 등급 불변, R1·R2 면 '재검토' 플래그) | 없음 |
| A·B 합산 상향 ≤ 2단계, 실제 변경 격자가 유니버스 5% 초과면 재점검 | 없음 |
| `reports/tables/grade_by_dong.csv` (행정동 × 등급 격자 수·인구) | 없음 |
| TOP 20 표에 `grade_raw`·`grade_final` 열 병기 | 없음 |

**지금 구현 가능한 것**: 위 표에서 규칙 C 와 하수센터별 집계를 뺀 나머지 전부.
규칙 C 는 자연재해위험개선지구·내수위험지구 13개소 좌표가 필요하고(팀원 작업 ③),
하수센터별 집계는 처리구역 경계가 필요하다.

**우선순위**: 9/9 에 먼저 한다. 등급 체계는 보고서 4장(등급 표·지도 범례)과
5장(대응 행동표)의 뼈대라, 나중에 바꾸면 그림·표를 다시 만들어야 한다.

---

## 주차별 일정

### 1주차 (9/8 ~ 9/13) — 9/14 를 맞을 준비

| 담당 | 할 일 | 기간 | 결과물 |
|---|---|---|---|
| 본인 | 승인 3건 | 9/8 | `artifacts/approvals/` 3개 |
| 본인 | **결정 003 등급 체계 R1~R5 구현** (위 절) | 9/9 |
| 본인 | 침수흔적 검증 코드 미리 작성 (ROC-AUC·상위 20% 포착률) | 9/10 | 9/14 에 파일만 넣고 실행 |
| 본인 | 사후검증(hindcast) 코드 — 2019년까지로 지수 만들고 2020~23 실제 침수 대조 | 9/10~11 | 보고서 첫 숫자가 될 것 |
| 본인 | XGBoost challenger 코드 (행정동 GroupKFold 공간 CV, SHAP) | 9/11~12 | `METHODOLOGY.md` §8-3 설계대로 |
| 본인 | 재현성 노드 `h10_reproducibility` | 9/12~13 | 마지막 미구현 필수 노드 |
| 팀원 | 홍수위험지도 SHP (창원 5개 구, 100년) | 9/9 | `data/raw/flood_maps/floodmap_go_kr/` |
| 팀원 | V-World 건물 — **지하층수·사용승인일 컬럼 유무를 먼저 확인** | 9/9~10 | `data/raw/buildings/` + 컬럼 목록 공유 |
| 팀원 | 자연재해저감계획 PDF (내수 위험지구 13개소 지번) | 9/11 | `data/raw/reference/` |
| 팀원 | 이의신청 (선택, 하려면 9/15 전) | 여유 시 | 접수번호를 access_log #18 에 |

**9/13 시점 목표**: 9/14 에 파일이 오면 `--only h06_layer1_flood` 한 줄로 검증이 붙는 상태.

### 2주차 (9/14 ~ 9/20) — 검증과 결과 확정

| 담당 | 할 일 | 기간 |
|---|---|---|
| 본인 | 침수흔적도 수령·형식 확인. 벡터가 아니면 대응 판단 | **9/14** |
| 본인 | Layer 1 외적 검증 (AUC ≥ 0.70, 상위 20% 포착률 ≥ 50%) | 9/15 |
| 본인 | 사후검증 실행 — "2020~23 실제 침수 N건 중 M건 포착" | 9/15~16 |
| 본인 | XGBoost 비교모델 + SHAP | 9/16~17 |
| 본인 | 결과 확인 EDA `h08_result_review` — 홍수위험지도·위험지구와 대조 | 9/17~18 |
| 본인 | Layer 3 에 건물 변수 추가 (팀원 자료 도착 시) → 전체 재실행 | 9/18~19 |
| 본인 | 최종 결과 확정, 그림·표 동결 | 9/20 |

**9/14 에 파일 형식이 PDF·csd 면**: 지번 디지타이징으로 회귀한다. 그 경우 XGBoost 는 포기하고
Layer 1 검증만 부분적으로 한다. 판단을 그날 바로 내린다.

### 3주차 (9/21 ~ 9/27) — 보고서

| 할 일 | 분량 |
|---|---|
| 1장 분석 개요 — 문제 정의, 2022 우수상 대비 차별화 비교표 | 2매 |
| 2장 데이터 — 출처 표, 전처리, 한계 | 2매 |
| 3장 분석 방법 — `METHODOLOGY.md` 를 압축 | 4매 |
| 4장 결과 — Layer 1~3, CDRI, 검증 수치, 우선대응 지역 | 6매 |
| 5장 활용방안 — 정책카드, 3개월 시범운영, 기대효과 | 3매 |
| 6장 출처·문헌 | 1매 |

체크리스트는 `docs/report_checklist.md`. 전 항목 ✓ 없이는 제출하지 않는다.

### 4주차 (9/28 ~ 9/30) — 마무리

| 할 일 |
|---|
| 팀원 교차 리뷰 (작성자 ≠ 리뷰어), 코멘트 반영 기록 |
| 노트북 01~05 전체 재실행 확인 |
| 파이프라인 전체 재실행 — 같은 결과가 나오는지 |
| 페이지 수 확인 (10~20매), 참가신청서 |
| **9/30 18:00 제출** |

---

## 남은 파이프라인 노드

20개 중 17개 통과. 남은 것은 3개다.

| 노드 | 상태 | 막는 것 |
|---|---|---|
| `h06_layer2_sewer` | optional, 미구현 | 배수구역 SHP + 행정동 조인. **없어도 CDRI 는 완결** (decision 001) |
| `h08_result_review` | 미구현 | 홍수위험지도 (팀원 9/9) |
| `h10_reproducibility` | 미구현 | 없음 — 지금 만들 수 있다 |
| `h09_alert_draft` | optional | 일정 여유 있을 때만 |

---

## 위험 요인

| 위험 | 가능성 | 대응 |
|---|---|---|
| 9/14 파일이 그림 형식(PDF·csd) | 중 | 그날 판단. Layer 1 검증만 부분 수행, XGBoost 포기 |
| 침수흔적 사건 수가 적어 AUC 불안정 | 중 | 상위 20% 포착률·lift 를 주지표로. AUC 는 참고 |
| 건물 자료에 지하층수 없음 | 중 | 건축HUB API 키 추가 신청 (팀원이 컬럼을 빨리 확인해야 하는 이유) |
| 보고서를 마지막 주에 몰아 씀 | 높음 | 3장은 `METHODOLOGY.md` 가 이미 초안이다. 2장 데이터 표도 지금 만들 수 있다 |
| 이의신청 인용 | 낮음 | 인용돼도 9/22 이후면 반영하지 않는다 |

---

## 지금 상태에서 이미 말할 수 있는 것

검증이 붙기 전이지만 이 숫자들은 이미 확정이다. 보고서에 그대로 쓸 수 있다.

| 항목 | 값 |
|---|---|
| 분석 격자 | 75,400칸 (순위 대상 10,202칸) |
| Layer 1 등급 | I 4,705 / II 7,897 / III 33,995 / IV 28,803 |
| 사례지 face-validity | lift **1.63** (기준 1.5) — 선행연구 현장조사 4개 행정동 |
| 변수 과의존 점검 | 하천·복개·예상도 제외 시 순위 상관 **0.904** |
| CDRI 강건성 | 중위 ρ 0.878 통과 / TOP20 중첩 9-20 미달 → **위험군(tier) 모드** |
| 강건 공통집합 | 8곳 (산식 4가지 모두의 상위 20) |
| 우선대응 지역 | 20곳, 인구 8,714명, 65세 이상 3,234명 |
| 연령 코드북 검증 | 노령화지수 항등식 상관 0.963, 대안 오차 10배 |
| 단위 테스트 | 110건 통과 |
