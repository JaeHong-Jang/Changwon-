# M4S 독립 검토 (읽기 전용)

판정: **PASS**

> 검토자(Claude Sonnet 5, 구현자와 다른 세션)가 `cloud/M4S` 브랜치를 읽기 전용으로 검토했다. 저장소 추적 파일은 수정하지 않았고(git commit/push 없음), 스크래치 스크립트는 `.omc/review_m4s/`(git 제외 경로, `.gitignore:64`로 확인)에만 뒀다가 검토 종료 후 삭제했다. 홀드아웃 원본(`data/raw/flood_traces/changwon_info_disclosure_20260924/`)과 `FT.load_holdout`·`src.models.holdout_eval`·옵션 없는 `m4_run`은 열거나 실행하지 않았다. `m4s_run` 전체 재실행 대신 `src.models.m4s_measure.generate`/`replicate`를 직접 호출해 소규모 재계산만 했다(공식 run 폴더는 건드리지 않음). 끝에 `git status --porcelain`이 빈 것을 확인했다.

## 확인한 것 (1~10)

**1. 시점.** `git log`: 절차 `66f8827`(17:48:13Z) → 구현 `3e8bc1c`(17:58:02Z) → AGENTS.md 표 `ef29ef0`(17:59:14Z) → 공식 run `84ebc39`(20:09:15Z) 순으로 커밋 시각이 증가한다. `git diff 66f8827 HEAD -- docs/q1/M4S_protocol.md`는 빈 출력이라 절차는 커밋 뒤 수정되지 않았다. `summary.json`의 `git_head: "3e8bc1c"`, `source_dirty: false`가 구현 커밋과 일치한다.

**2. 코드 대 절차.** 파일별로 절차 문면과 대조했다. 격자(200×200, origin (1e6,1.7e6), 100 m, `src/synth/landscape.py:7-9`), 배경 평활(σ=2칸·wrap·표준화, `landscape.py:30-36`), 크기 분포·ζ(로그정규·Pareto 모두 생성 분포 기준 정확히 평균0·표준편차1, `inventory.py:15-34`—Pareto는 `-ln(1-U)-1` 형태로 Exp(1)-1과 동치임을 유도로 확인), 절단 [50, 1e7] m²(`inventory.py:7-8`), 타원(반축 공식이 "π 공식으로 만든 뒤 32각형 실제 면적으로 재조정"과 대수적으로 완전히 동치임을 유도로 확인, `inventory.py:48-69`), 사상별 객체 수(`equal`/`skewed`, `inventory.py:37-45`), 탐지력 식(`detectability.py:10-16`), 점수 조립(겹침 최대 객체·동점 최소 번호를 `lexsort((obj,-area,cell))`로 정확히 구현, `detectability.py:19-28`), 난수(`default_rng([SEED, no, j])`, 독립 재현 확인), 시나리오 115개와 순서(묶음별 60/24/12/9/10, 각 묶음의 "바깥 고리" 순서까지 표대로, `scenarios.py:38-45`, 테스트로도 확인), 라벨 규칙·배경 정의(M4 `label_rules.py`/`trace_footprint.py` 그대로 재사용), 측정 항목·최소 표본·분류(P10 경계 0.1/0.5)·게이트 정의·판정 P2/P3/P3′·보조 S1~S6이 모두 절차 문면과 일치했다. 어긋남은 찾지 못했다.

**3. 수학.** §5.2 공분산 형태를 직접 유도했다: S 위에서 `Σ(w̃_k−1/n_S)(A_k−Ā) = Σw̃_kA_k − Ā`(가중치 합=1 이용)이고, `n_S·sd(w̃) = √(n_S/n_eff,S−1)`을 population-variance 정의로 전개해 확인, 두 식을 결합하면 절차의 곱 형태(ρ·sd·√(n_S/n_eff,S−1))가 정확히 나온다. 코드(`weight_concentration.py:11-31`)의 `direct`/`product` 두 계산이 이 유도와 그대로 일치한다. §5.3 Δ*도 독립적으로 유도·재계산했다(아래 표 참조): μ0=0.95, τ=0.5의 (β,σ) 표 30칸 전부 protocol 값과 소수 셋째 자리까지 일치, D묶음(μ0=0.55/1.45)·상위10%몫·n_eff/n 극한값도 전부 일치. 가정(가중치∝면적, 절단 없음, n→∞)과 σ≥2.5에서 과대평가 경고도 정확하다.

**4. 독립 재계산.** `m4s_measure.generate`/`positive_mass`로 원자료를 재현한 �3, 프로젝트 내부 `weighted_auc.py`/`decomposition.py`를 쓰지 않고 별도 스크립트(sklearn `roc_auc_score` doubling-trick, 직접 shapely 교차 + 수기 mid-CDF)로 7개 조합을 재계산했다(f10·soft·rep_point 포함, A묶음 고σ 시나리오 1개(no=59, σ=3·β=1), B묶음 사상별 행 4개). 전부 부동소수 오차(1e-16 수준) 안에서 공식 값과 일치했다(아래 표). 한 가지 유의미한 확인: B묶음 사상별 객체 AUC는 배경을 "그 사상만의 비접촉 칸"으로 잘못 잡으면 0.002~0.003 차이가 났는데, 절차 §3의 "시나리오의 어떤 객체와도 닿지 않은 칸"(사상이 아니라 시나리오 전체)으로 배경을 바로잡으니 diff=0.0이 됐다 — 즉 공식 코드는 이 미묘한 정의를 정확히 지켰고, 내 첫 시도 쪽이 틀렸던 것이었다. 생성기 자체(면적분포·ζ)는 위 2번 유도로 수식 동치를 확인했고, 별도 Monte Carlo 재구현까지는 하지 않았다(한계에 기재).

**5. 판정 재현.** `summary_by_scenario.csv`만으로 P2·P3·P3′를 별도 구현으로 재적용한 결과 `decision.json`과 완전히 일치했다(P2: True/42/0, P3: True/15/8/0, P3′: True/15/8/0). `replicates.parquet`에서 3개 시나리오(no=19 soft, no=59 f10, no=0 f10)의 중앙값·P10·중앙값 구간을 독립 재집계해도 소수 10자리까지 일치했다.

**6. 창원 기준점.** `anchors.csv`의 `HOLDOUT_ALL`/L1 행(σ̂=2.46, β̂=−0.80, Δ̂*=−0.406, 관측 −0.276)을 M4 공식 산출(`object_contrib.parquet`의 `A_gate`/`A_object` 컬럼이 `covariance_form`의 A/At 인자와 정확히 대응함을 `m4_metrics.py:75-77`에서 확인)에서 재현 가능함을 확인했다. 해석 한계(τ̂에 표본 잡음 혼입, 인과 아님, post-hoc)는 절차 §7과 보고서 한계 절 양쪽에 명시돼 있다.

**7. 단위 검사.** `env -u CHANGWON_HOLDOUT_TESTS PYTHONPATH=. .venv/bin/python -m unittest discover -s tests` → 425개 전부 통과(skip=2). `tests/test_m4s.py`(21개, AST로 확인)는 스모크 테스트가 아니라 실질 성질(절단 범위·ζ 공식, 타원 넓이 정확성, 동점 처리, 공분산 형태 항등식, Δ* 부호·MC 근사, 중앙값 구간 순위, P3 구역 로직, 기준점 파라미터 복원)을 검사한다.

**8. §6 코드 규칙.** 12개 M4S 관련 파일 전부 AST로 확인: 모든 함수/클래스 docstring이 한 줄, 여러 줄 docstring 없음. 코드 문단마다 한 줄 주석이 실제로 달려 있다(육안 확인). 실행 흐름(`m4s_run.py`)과 계산(`m4s_measure.py` 등)이 분리돼 있고 기능별로 새 파일이다. `AGENTS.md` §6 표에 M4S 행이 추가돼 있다(`ef29ef0`).

**9. 표현 규칙.** `docs/CLOUD_TASKS.md` 금지 표현(전향적·사전등록 검증, RF가 검증됐다, 10사상 일관, ML이 지침보다 우월, 강수 역전 원인 확정, CDRI가 우선순위를 식별한다) 6개를 절차·코드·`docs/q1/M4S.md`에서 grep했으나 매치 없음.

**10. 설계의 약점(보고서 자체가 이미 인정).** (a) 객체 신호를 닿은 칸 전체에 동일 크기로 주므로 "객체 안" 항이 구성상 거의 0(−0.001~−0.011)이라 실제 자료의 가장자리 희석 효과는 이 시연으로 말할 수 없다. (b) 객체 배치가 공간적으로 완전 무작위(저지대 군집 없음), 배경 점수장이 정상 가우스장 하나뿐이다. (c) 결합을 로그면적 선형 탐지력 이동 하나로만 넣어, 사상마다 β가 다른 창원의 실제 패턴(2024-09-20 한 사상만 강한 음의 결합, 나머지는 |β̂|≤0.17)은 시뮬레이션하지 못해 V1–V2 대비가 재현되지 않는다(보고서가 이 실패를 숨기지 않고 그대로 보고함). (d) σ 상한 3.0·중앙면적 5,000 m²·μ0=0.95는 M4 결과를 보고 정한 값(절차 §0에 공개, 이해충돌 아님). 이 네 가지는 결과 해석(특히 "일반성" 주장의 범위)을 제한하는 실질적 약점이며, 보고서 한계 절이 정확히 이 항목들을 다루고 있어 과대해석 위험은 낮다.

## 지적

[낮음] `src/models/m4s_decision.py:84-95` (S1, S2) — S1·S2가 β=0/erased 대상 블록을 `["A","C1","C2","D"]`로 한정해 B묶음의 β=0 행을 뺐다. 절차 §6.2 문면(“β=0 시나리오”, “묶음 A·C1”)은 B 포함 여부가 모호하다. 결과(판정)에는 영향 없음(S1/S2는 보조 예측이고 실제로도 pass). 보고서나 절차에 "B는 제외"라고 한 줄 명시하면 다음 리뷰에서 같은 질문이 안 나온다.

[낮음] 사전 파일럿 실행(복제 2개)의 흔적 — `.gitignore`에 `artifacts/q1/M4S` 관련 특칙이 없어, 커밋 전에 지운 미기록 산출물은 git에 전혀 남지 않는 것이 정상이다(디스크·reflog 전수 검색에서도 흔적 없음). 리더의 진술과 모순되는 증거는 없지만, 이 방식으로는 "정말 실행했는지"를 사후에 독립 검증할 수 없다는 구조적 한계가 있다(고치라는 게 아니라 기록용 지적).

[정보] 그 외 코드·수학·산출물 대조에서 불일치를 찾지 못했다.

## 독립 재계산 표

| 항목 | 공식 값 (replicates.parquet) | 재계산 값 (별도 스크립트) | 차이 |
|---|---:|---:|---:|
| 격자AUC no=0 rep=0 f10 (n=30) | 0.5194636665352581 | 0.5194636665352582 | 1.1e-16 |
| 격자AUC no=0 rep=0 soft (n=30) | 0.450245140480957 | 0.45024514048095654 | 4.4e-16 |
| 격자AUC no=0 rep=0 rep_point (n=30) | 0.613999666416479 | 0.6139996664164791 | 1.1e-16 |
| 격자AUC no=19 rep=3 soft (σ=1.0,β=1,n=200) | 0.8469574931588403 | 0.84695749315884 | 3.3e-16 |
| 격자AUC no=59 rep=7 f10 (σ=3.0,β=1,n=200) | 0.9469373795958902 | 0.9469373795958901 | 1.1e-16 |
| 객체AUC no=0 rep=0 (n_obj=30, 직접 shapely 교차) | 0.6140355112236727 | 0.6140355112236727 | 0.0 |
| B묶음 사상별 객체AUC (no=60,rep=2,event0~3, 시나리오 전체 비접촉 배경) | 0.7015/0.7077/0.8080/0.6851 | 동일(4자리 일치) | 0.0 |
| P2 판정 | pass=True, n=42, fail=0 | pass=True, n=42, fail=0 | 0 |
| P3(soft) 판정 | pass=True, 15/8/0 | pass=True, 15/8/0 | 0 |
| P3′(f10) 판정 | pass=True, 15/8/0 | pass=True, 15/8/0 | 0 |
| §5.3 Δ*(μ0=0.95,τ=0.5) 30칸 표 | protocol §5.3 표 | 독립 재계산 | 전부 일치(소수 3자리) |
| 시나리오 요약 재집계 (no=19/59/0, gap_median·p10·구간) | summary_by_scenario.csv | replicates.parquet에서 독립 재집계 | 소수 10자리 일치 |

**한계·미확인:** 생성기(면적분포·ζ) 자체는 대수적 유도로 동치를 확인했을 뿐, 별도 난수 시드로 통계적(몬테카를로) 재구현까지는 하지 않았다. 대형 시나리오(n_obj=200)의 객체AUC는 sklearn·기존 경로 교차확인은 했으나, 40,000칸×200폴리곤 전체를 shapely로 처음부터 다시 교차시키는 완전 독립 기하 재현은 시간 예산상 n_obj=30 시나리오 1개로만 했다(그 결과는 정확히 일치). `docs/q1/M4S.md`는 검토 시작 시점엔 없었으나 검토 도중 리더가 커밋(`dac08da`)했고, 표 안 숫자 다수(P2 42행·최대오차, gap_map 6×2행, 분류별 벌어짐 개수 8종)를 공식 산출물과 직접 대조해 전부 일치함을 확인했다(전문 검토는 아님, 발췌 대조).

---

## 리더 메모 (2026-09-26, 검토 원문 뒤에 덧붙임)

- 검토 원문은 서브 에이전트가 돌려준 글 그대로다. 4번 항목의 깨진 글자 한 개("재현한 �3")는 전달 과정에서 생긴 것으로, 뜻은 "재현한 뒤"다. 고치지 않고 남겼다.
- 지적 1 (S1·S2 의 B 묶음): S2 는 절차 문면이 "묶음 A·C1" 이라 B 가 대상이 아니다. S1 은 문면이 모호해 구현이 B 를 뺐다.
  B 를 넣어 다시 봤다: B 의 β = 0 시나리오 8개(`f10`)는 P10 이 모두 0.005 이하라 S1 결과(맞음)는 같다. 보고서 보조 예측 절에 적었다. 절차는 고치지 않는다.
- 지적 2 (점검 실행 흔적): 받아들인다. 점검 실행의 run_id 는 `m4s_partial_20260926T175517Z_66f8827-dirty` 였고, 산출물은 공식 실행 전에 지웠다 (보고서 증거 절).
- 검토 뒤 그림만 다시 그렸다: 원래 `gap_map.png` 는 칸 글자가 옆 칸과 붙어 읽기 어려웠다. `src/models/m4s_figure.py` 의 글자 형식만 고쳐 같은 `summary_by_scenario.csv` 에서
  `gap_map_redrawn.png` 를 만들었다. 수치는 바뀌지 않았고, 공식 run 이 만든 `gap_map.png` 는 그대로 둔다.
