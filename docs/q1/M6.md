# M6 보고 — 재현성 정리 (클라우드 항목)

> 절차: `docs/q1/M6_protocol.md` (커밋 `b834248`, 재실행 전). 과제: `docs/CLOUD_TASKS.md` M6 중 클라우드 항목.
> 파이프라인 재생성과 `SHA256SUMS` 비교는 로컬 과제라 하지 않았다.
> 공식 재실행: `PYTHONPATH=. .venv/bin/python -m src.models.holdout_eval` → run `holdout_20260926T164805Z_7f7968f_84ef30a7`
> (소스 clean, HEAD `7f7968f`). 비교·감사·연대표·목록: `PYTHONPATH=. .venv/bin/python -m src.repro.m6_run`
> → `artifacts/q1/M6/m6_20260926T164903Z_7f7968f/` (단계별 명령과 git HEAD 는 그 폴더의 `summary.json` → `steps`).
> 이 보고의 수치는 모두 이 두 실행의 산출에서 나왔다.

## 결과 요약

- **홀드아웃 재실행: 사전 규칙 판정은 "불일치"다.** 게이트 해시(J1)는 9/24 인용 실행과 같다. 게이트 10개 점수의 격자 AUC·덩어리 AUC·포착률은 모두 같다
  (L1 0.4357 / 0.6025 / 0.0734, 탈락). 지표표 5,670행 가운데 면적·객체 단위 1,966행이 최대 1.8e-12 다르다(J2).
  실행 메타 `union_area_km2` 도 2.0e-13 km² 다르다(J3). 원인은 흔적 재투영 좌표의 마지막 자리 차이로 추정한다.
- **run_id 정정 완료.** 인용 수치 10개가 저장 요약(`…104040Z_1b8106ba`, `…104628Z_1b8106ba`)과 모두 같다. 그래서 `PAPER_ROADMAP.md` §2 와
  `METHODOLOGY.md` §7.6 의 run_id 를 바꿨다. 라벨 감사를 다시 돌려도 요약은 같다. 폴리곤 표는 `object_id` 9·47행이 달라 규칙상 불통과다(같은 원인).
- **동결 연대표 42행, 모두 출처 확인.** L1 산식은 홀드아웃 입수 16일 전(9/8 00:50 KST까지)에 마지막으로 바뀌었다.
  홀드아웃은 첫 공식 채점(9/24 18:38 KST) 전인 18:18 KST까지 이미 읽혔다. 특징 후보군을 정한 진단 시각은 기록이 없다.
  Zenodo 후보 458개 파일 가운데 open 340, restricted 5, hash_only 111, exclude 2 다. 라이선스 파일은 없다.

## 변경 파일

| 경로 | 설명 |
|---|---|
| `docs/q1/M6_protocol.md` | 계산 전 절차: 판정 규칙 J1~J3, run_id 정정 기준, 연대표 출처 순위, 기탁 분류 규칙 |
| `src/repro/run_compare.py` | 두 홀드아웃 실행의 게이트 해시·지표표·메타 비교와 판정 |
| `src/repro/audit_check.py` | 라벨 감사 재실행 산출과 저장본 비교 |
| `src/repro/chronology.py` | 연대표 사건 시각을 JSON 키·run_id·git 커밋·gpkg·문서 인용에서 읽기 |
| `src/repro/deposit_manifest.py` | git 추적 파일 분류·해시 목록 |
| `src/repro/m6_run.py` | 실행: 비교 → 감사 재확인 → 연대표 → 목록, 단계별로 이어 쓰기 |
| `config/m6_chronology.yaml` | 연대표 사건 명세 42행 (시각 없음, 출처만) |
| `tests/test_m6.py` | 합성 자료 단위 검사 12개 (홀드아웃 없음) |
| `AGENTS.md` | §6 기능별 파일 표에 M6 행 추가 |
| `docs/PAPER_ROADMAP.md` | 라벨 감사 run_id 정정, 재실행 병기, P0-8 상태 |
| `docs/METHODOLOGY.md` | §7.6 라벨 감사 run_id 정정, 재실행 병기 |
| `docs/q1/M6_chronology.md`, `docs/q1/M6_zenodo.md` | 연대표, 기탁 목록 |
| `docs/q1/M6_review.md` | 독립 교차 리뷰(다른 모델, 읽기 전용) PASS-with-fixes 와 리더 조치 |
| `artifacts/evaluation/holdout_2022_2024/holdout_20260926T164805Z_7f7968f_84ef30a7/`, `latest.json` | 공식 재실행 산출 (코드가 정한 위치) |
| `artifacts/q1/M6/m6_20260926T164903Z_7f7968f/` | `holdout_compare.json`, `metrics_diff.csv`, `label_audit/`, `label_audit_compare.json`, `chronology.csv`, `zenodo_manifest.csv`, `zenodo_summary.json`, `summary.json` |

`config/holdout_reference.yaml` 은 바꾸지 않았다 (절차 §2.4: 재현이 아니면 그대로 둔다).

## 증거

### A. 홀드아웃 재실행 (`holdout_compare.json`)

실행 전 확인(절차 §1): 원본 흔적 47개 파일 해시는 9/24 와 같다. 스냅샷 해시 4개도 일치한다(`cloud_setup.sh`). `src`·`config` 에는 미커밋 변경이 없었다.

| 규칙 | 주 기준 `…100939Z…cf388a23` | 보조 기준 `…104608Z…4af97d5d` |
|---|---|---|
| J1 게이트 해시 (`48d83b93…`, `2ea2f057…`) | 같음 | 같음 |
| J2 행·키 | 5,670 = 5,670, 한쪽에만 있는 키 0, 범주 열 불일치 0 | 같음 |
| J2 수치 | 1,966행 다름 (area 1,163, object 803), 최대 절대차 value 1.82e-12, ci_lo 1.37e-12, ci_hi 8.46e-13, 최대 상대차 6.5e-10 | 같음 |
| J3 실행 메타 | `holdout.union_area_km2` 9.546202081806701 ≠ 9.546202081806902. 나머지(사상별 폴리곤 수, 배경 72,953칸, 원본·모델 해시, 역할) 같음 | 같음 |
| **판정 (§2.3)** | **불일치** | **불일치** |

게이트 값 (새 실행, 9/24 인용 실행과 소수 4자리까지 같은 값):

| 점수 | 격자 AUC | 덩어리 AUC | 상위 20% 포착 | 게이트 | 9/24 대비 |
|---|---:|---:|---:|---|---|
| L1 | 0.4357 | 0.6025 | 0.0734 | 탈락 | 같음 |
| model_frozen | 0.8583 | 0.8255 | 0.8571 | 통과 | 같음 |
| model_frozen_to2019 | 0.8432 | 0.8211 | 0.7632 | 통과 | 같음 |
| rf_F0_v1 | 0.8761 | 0.8337 | 0.8666 | 통과 | 같음 |
| rf_F0_v1_to2019 | 0.8508 | 0.8287 | 0.7198 | 통과 | 같음 |
| city_flood_map | 0.4501 | 0.4941 | 0.0260 | 탈락 | 같음 |
| past_flood_pre2022 | 0.5322 | 0.5682 | 0.2052 | 탈락 | 같음 |
| L1_without_flood_map | 0.4387 | 0.6053 | 0.0781 | 탈락 | 같음 |
| z_sensitivity | 0.7348 | 0.7616 | 0.2249 | 탈락 | 같음 |
| z_exposure | 0.1468 | 0.3459 | 0.0158 | 탈락 | 같음 |

기록만 하는 차이 (판정 제외): git HEAD `ae9c122`(dirty) → `7f7968f`(clean). Python 3.14.4 → 3.12.3, numpy 2.5.2 → 2.5.3, pandas 3.0.5 → 3.0.6.
나머지 패키지는 같은 판이다. 코드 해시는 보조 기준 대비 `src/models/provenance.py` 하나만 다르다.
9/24 실행 때의 판(`d6a25cf0…`)은 커밋된 적이 없어 내용을 직접 비교할 수 없다. 첫 커밋(`fd5f86d`, 9/24 21:08 KST)과
지금의 차이는 평가 경로가 쓰지 않는 `atomic_write_json` 추가뿐이다.
주 기준 대비로는 파일 구성이 다르다(`validation.py` → `validation/` 패키지 분리, 흔적 모듈 분리).
가공 자료 해시는 `layer1_flood.gpkg` 만 다르다(9/24 `3f5fb14e…`, 스냅샷 `0a731462…`; 스냅샷 파일은 2026-09-24T16:18:26Z 에 다시 쓰였다).

**원인 분석 (추정, post-hoc 진단):**
- 라벨 감사 재실행에서 `object_id`(역할·호우·정규화 WKB 의 SHA-256)가 바뀐 폴리곤과 면적이 바뀐 폴리곤이 정확히 같다.
  개발 9/145(최대 3.4e-8 m²), 홀드아웃 47/196(최대 1.5e-7 m²)이다(`label_audit_compare.json`, 폴리곤 표 비교).
- 개발 쪽 변화 9개는 모두 2025-07-19 사상이다. 이 사상의 창원시 NDMS 파일은 EPSG:5186 이라 EPSG:5179 로 재투영된다.
  원래 EPSG:5179 인 행안부 API 폴리곤 125개(2006~2019)는 하나도 바뀌지 않았다. 홀드아웃 파일은 모두 EPSG:5186/5187 이다.
- 그래서 좌표 변환(pyproj/PROJ)의 마지막 자리 차이가 가장 유력하다. 이 차이가 폴리곤 면적 → 합집합 면적 → 면적·객체 단위 지표로 번졌다.
  격자·덩어리 단위 지표는 격자 라벨이 같아 바뀌지 않았다.
  이 세션은 pyproj 3.8.0, PROJ 9.8.1, GEOS 3.13.1 이다. 9/24 실행은 pyproj·PROJ·GEOS 판을 기록하지 않아(`package_versions`) 확정할 수 없다.

### B. run_id 정정 (`label_audit_compare.json`)

| 인용 값 | 문서 | 저장 요약 | 일치 |
|---|---:|---:|---|
| 개발 n / 생존율 / 면적 중앙값 | 145 / 95.2% / 8,750 m² | 145 / 0.952 / 8,750.1 | 예 |
| 홀드아웃 n / 생존율 / 면적 중앙값 | 196 / 66.3% / 3,590 m² | 196 / 0.663 / 3,589.6 | 예 |
| 홀드아웃 도심 n / 생존율 | 57 / 12.3% | 57 / 0.123 | 예 |
| 홀드아웃 농경지 n / 생존율 | 119 / 97.5% | 119 / 0.975 | 예 |

- 정정: `label_audit_development_20260924T095654Z_40523c56` → `label_audit_development_20260924T104040Z_1b8106ba`,
  `label_audit_holdout_20260924T095656Z_40523c56` → `label_audit_holdout_20260924T104628Z_1b8106ba`.
  `PAPER_ROADMAP.md` §2 와 `METHODOLOGY.md` §7.6 두 곳이다. `grep` 으로 찾은 나머지 인용 3곳은 고치지 않았다.
  `Q1_review.md`·`CLOUD_TASKS.md` 는 불일치 자체를 기록한 문서다. `research_reports/F2.md:54` 는 조사 보고서 원문이라 그대로 둔다.
- 재실행(산출 폴더만 M6 로 바꿈, 저장본 불변): 요약 JSON 은 `run_id` 를 빼면 두 역할 모두 같다. 코드 해시도 `1b8106ba` 로 같다.
  폴리곤 표의 규칙 열(`rule_*`)·정수 열·사상 열은 같다. 실수 열 최대차는 1.5e-7 로 허용 1e-6 안이다.
  `object_id` 는 개발 9행·홀드아웃 47행이 달라 절차 §3.2 규칙상 **불통과**다. 원인은 A 와 같다.
- `40523c56` 은 저장된 산출이 없어 확인하지 못했다. `label_audit.py` 이전 판본의 코드 해시이고, 같은 경로에 쓴 뒤 실행이 산출을 덮어썼다고 **추정**한다.
  현재 `label_audit.py` 는 `22211b4`(첫 커밋)부터 `1b8106ba` 다.

### C. 연대표 → `docs/q1/M6_chronology.md`

42행이 모두 확인됐다(`chronology.csv` `verified`). 입력 8, 동결 8, 입수 5, 최초 채점 3, 설계 변경 13, 재실행·검증 5 이다.

### D. Zenodo 목록 → `docs/q1/M6_zenodo.md`

### 단위 검사

`env -u CHANGWON_HOLDOUT_TESTS .venv/bin/python -m unittest discover -s tests` → 370개 통과(2개 skip). 기존 358개에 M6 12개를 더한 수다.

### 교차 리뷰 → `docs/q1/M6_review.md`

판정 PASS-with-fixes. 검토자가 J1·J2·J3·게이트 표·라벨 감사·연대표·목록 합계를 저장 산출에서 독립 재계산했고 모두 이 보고와 같았다.
지적 3건(중간 1: 정수 열 업캐스트 때 허위 불일치, 낮음 2)은 고쳤다. 수정한 코드로 compare·chronology 단계를 다시 돌렸다.
`holdout_compare.json`·`metrics_diff.csv` 는 바이트까지 같았다. 그래서 `summary.json` 의 compare 단계 `git_head` 는 수정 커밋을 가리킨다.
audit 단계(홀드아웃을 읽음)는 다시 돌리지 않았다. 대신 저장된 재실행 CSV 에 수정 함수를 적용해 결과가 같음을 확인했다.

## 한계·미확인

- 재실행 판정의 원인(재투영 마지막 자리)은 추정이다. 9/24 환경의 pyproj·PROJ·GEOS 판을 모른다.
- `layer1_flood.gpkg` 는 9/24 실행과 바이트가 다르다. 그래도 격자·덩어리 지표와 게이트 해시는 같다. 내용이 같은지는 로컬에서 9/24 파일이나 파이프라인 재생성으로 확인해야 한다.
- 연대표 S0(특징 후보군을 정한 홀드아웃 진단)은 시각 기록이 없다. `.omc/review/RV1.md`, v1 `unittest.log`, Codex 보고서처럼 로컬에만 있는 기록은 보지 못했다.
- 커밋 시각은 상한이다. 문서 날짜는 KST 날짜 정밀도다.
- Zenodo 목록 해시는 목록 생성 시점 기준이다. 목록 뒤에 고친 파일 5개(`M6.md`, `M6_zenodo.md`, 실행 폴더의 `summary.json`·`zenodo_manifest.csv`·`zenodo_summary.json`)는 해시가 다르다.
  cloud-base 에 없는 LFS 원본은 목록에 없다.
- 이용 조건(라이선스)은 저장소 기록만 봤다. 기관에 확인하지 않았다.

## 리더 판단 필요

1. **재실행 판정:** 사전 규칙상 불일치라서 인용은 9/24 실행으로 두었다. 문서에는 재실행을 "게이트 해시 동일, 규칙상 불일치"로 병기했다.
   J3 를 정확 일치로 정한 것은 실수형 메타(`union_area_km2`)를 예상하지 못한 절차의 빈틈이다. 이 빈틈을 지금 고치면 post-hoc 이다.
   다음 절차부터 실수 메타에 허용오차를 두는 것을 제안한다.
2. **`object_id` 안정성:** 좌표 마지막 자리에 따라 환경마다 바뀐다. 지표 계산에는 영향이 없다(묶음 키로만 쓴다).
   그래도 객체 식별자로 인용하려면 좌표를 반올림한 뒤 해시하거나 `source_record_id` 를 써야 한다. ID 가 바뀌는 코드 변경이라 별도 과제로 둔다.
3. **실행 기록 보강:** `provenance.package_versions` 에 pyproj·PROJ·GEOS 판을 넣으면 이번 같은 원인을 확정할 수 있다.
   다만 `holdout_eval` 코드 해시가 바뀐다.
4. **`latest.json`:** 코드가 새 실행을 가리키게 바꿨다. `holdout_reference.yaml` 은 9/24 run_id 그대로다. H10 불변 검사는 게이트 해시로 판정하므로 통과한다.
   develop 에 가져올 때 `latest.json` 을 새 실행으로 둘지 정한다.
5. **Zenodo:** 라이선스 파일 없음. 창원시·행안부·공공데이터 원본의 재배포 조건 확인. 가공 스냅샷 공개 범위. 저장소 공개 여부
   (cloud-base 는 홀드아웃 원본을 일반 git 파일로 담는다). `docs/q1/M6_zenodo.md` §3.
6. **로컬 과제 연결:** 로컬 파이프라인 재생성 때 9/24 `layer1_flood.gpkg`(`3f5fb14e…`)와 스냅샷(`0a731462…`)의 내용 비교를 같이 해 주면 A 의 입력 차이가 닫힌다.
